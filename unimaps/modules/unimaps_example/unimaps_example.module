<?php

function unimaps_example_unimapsapi(&$object, $op, $type) {

  // Изменение параметров карты и геообъектов на ней перед выводом
  if ($op == 'map') {

    // Здесь можно в массиве $object->options изменять ЛЮБЫЕ параметры
    // карты и находящихся на ней геообъектов ПЕРЕД их выводом на карту
    // Также можно при необходимости ДИНАМИЧЕСКИ добавлять или удалять
    // с карты необходимые геообъекты

    /**  см. dpm($object->options); **/

  }

}
